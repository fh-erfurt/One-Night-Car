CREATE TABLE EMPLOYEE (ID BIGINT AUTO_INCREMENT NOT NULL, DTYPE VARCHAR(31), CREATED DATETIME, DATEOFBIRTH DATETIME, EMPLOYEEID INTEGER, FIRSTNAME VARCHAR(255), MODIFIED DATETIME, SALARY FLOAT, SURNAME VARCHAR(255), TYPEOFACTIVITY INTEGER, VERSION BIGINT, PERSONADDRESS_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE PERSONADDRESS (ID BIGINT AUTO_INCREMENT NOT NULL, ZIP VARCHAR(255), CITY VARCHAR(255), CREATED DATETIME, MODIFIED DATETIME, STREET VARCHAR(255), STREETNUMBER VARCHAR(255), VERSION BIGINT, PRIMARY KEY (ID))
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_PERSONADDRESS_ID FOREIGN KEY (PERSONADDRESS_ID) REFERENCES PERSONADDRESS (ID)
